# راهنمای کامل ساخت، تست و انتشار «شتر جهنده» در کافه‌بازار

این پروژه با فناوری **Cordova** ساخته شده: یک بازی HTML/JavaScript که داخل یک برنامه اندرویدی واقعی بسته‌بندی می‌شود. نیازی به دانش برنامه‌نویسی اندروید نداری؛ فقط این مراحل را دقیق دنبال کن.

---

## بخش صفر: تست فوری بازی (بدون نصب هیچ‌چیز)

قبل از هر کاری، فایل `www/index.html` را با دوبار کلیک در مرورگر (Chrome) باز کن. بازی کامل همان‌جا اجرا می‌شود. این کار را هر بار بعد از تغییر کد تکرار کن تا سریع تست کنی.

---

## بخش ۱: نصب ابزارهای لازم (فقط یک‌بار)

۱. **Node.js** را نصب کن: به سایت nodejs.org برو، نسخه LTS را دانلود و نصب کن.
۲. **Java JDK 17** را نصب کن (برای اندروید ۱۳/۱۴ لازم است): از سایت Adoptium (Temurin JDK 17) دانلود کن.
۳. **Android Studio** را از developer.android.com/studio نصب کن. بعد از نصب، آن را باز کن و از منوی `More Actions > SDK Manager`:
   - `Android SDK Platform 34` را نصب کن.
   - در تب `SDK Tools`، گزینه‌ی `Android SDK Build-Tools` و `Android SDK Command-line Tools` را تیک بزن و نصب کن.
۴. متغیرهای محیطی را تنظیم کن (ویندوز):
   - `ANDROID_HOME` = مسیر پوشه SDK (معمولاً `C:\Users\نام‌شما\AppData\Local\Android\Sdk`)
   - به `PATH` این مسیرها را اضافه کن: `%ANDROID_HOME%\platform-tools` و `%ANDROID_HOME%\cmdline-tools\latest\bin`
   - در مک/لینوکس همین متغیرها را در فایل `~/.zshrc` یا `~/.bashrc` اضافه کن.
۵. یک ترمینال جدید باز کن و بررسی کن همه چیز نصب شده:
   ```
   node -v
   java -version
   ```
۶. Cordova را سراسری نصب کن:
   ```
   npm install -g cordova
   ```

---

## بخش ۲: اجرای پروژه

۱. پوشه‌ی `camel-run` (همین پوشه‌ای که دریافت کردی) را جایی روی سیستم قرار بده.
۲. در ترمینال وارد همان پوشه شو:
   ```
   cd مسیر/پوشه/camel-run
   ```
۳. پلتفرم اندروید را اضافه کن:
   ```
   cordova platform add android
   ```
۴. یک گوشی اندرویدی وصل کن (با فعال بودن «USB Debugging» در تنظیمات توسعه‌دهنده) یا یک شبیه‌ساز (Emulator) در Android Studio بساز.
۵. برای اجرای مستقیم روی گوشی:
   ```
   cordova run android
   ```

---

## بخش ۳: برطرف کردن خطاهای رایج

- **خطای `ANDROID_HOME is not set`**: مرحله ۴ بخش ۱ را دوباره چک کن؛ بعد از تغییر متغیرها، ترمینال را ببند و دوباره باز کن.
- **خطای `JAVA_HOME`**: مسیر نصب JDK 17 را در متغیر محیطی `JAVA_HOME` تنظیم کن.
- **خطای گم‌شدن `Gradle`**: اولین اجرا کمی طول می‌کشد تا Gradle دانلود شود؛ اینترنت باید وصل باشد. صبر کن تا کامل شود.
- **`cordova: command not found`**: دستور `npm install -g cordova` را دوباره اجرا کن و مطمئن شو npm به PATH اضافه شده.
- **صفحه سفید در اجرا**: مطمئن شو فایل `www/index.html` دقیقاً همین نامی که هست را دارد و داخل `config.xml` خط `<content src="index.html" />` تغییر نکرده.

---

## بخش ۴: تنظیم آیکون، نام، نسخه و اطلاعات برنامه

همه‌ی این‌ها داخل فایل `config.xml` هستند:

- **نام برنامه**: تگ `<name>شتر جهنده</name>` را به دلخواه تغییر بده.
- **شناسه بسته (Package ID)**: در ابتدای فایل، `id="ir.yourname.camelrun"` — این را به چیزی یکتا و مخصوص خودت تغییر بده، مثلاً `ir.mahdi.camelrun` (بعد از انتشار دیگر قابل تغییر نیست).
- **نسخه برنامه**: `version="1.0.0"` — هر بار که آپدیت جدید در کافه‌بازار می‌گذاری، این عدد را افزایش بده (مثلاً 1.0.1).
- **توضیحات و نویسنده**: تگ‌های `<description>` و `<author>` را ویرایش کن.

**آیکون برنامه:**
۱. یک تصویر مربعی ۱۰۲۴×۱۰۲۴ پیکسل (PNG) طراحی کن یا سفارش بده.
۲. آن را با نام `icon.png` داخل پوشه `res/` این پروژه بگذار (اگر پوشه `res` نیست بسازش).
۳. در ترمینال، داخل پوشه پروژه دستور زیر را بزن تا همه سایزها خودکار ساخته شود:
   ```
   npx cordova-res android --skip-config --copy
   ```

---

## بخش ۵: ساخت فایل APK

۱. داخل پوشه پروژه:
   ```
   cordova build android
   ```
۲. فایل خروجی (نسخه آزمایشی/دیباگ) اینجا ساخته می‌شود:
   ```
   platforms/android/app/build/outputs/apk/debug/app-debug.apk
   ```
این فایل را می‌توانی مستقیم روی گوشی نصب و تست کنی، اما برای انتشار در کافه‌بازار باید نسخه‌ی «امضا شده» (Release) بسازی — به بخش بعد برو.

---

## بخش ۶: ساخت نسخه‌ی نهایی امضا شده (Release APK / AAB) برای کافه‌بازار

کافه‌بازار هم APK و هم AAB را می‌پذیرد؛ AAB حجم کمتری برای کاربر دارد و توصیه می‌شود.

### مرحله ۱: ساخت کلید امضا (Keystore) — فقط یک‌بار برای همیشه
```
keytool -genkey -v -keystore camelrun-release.keystore -alias camelrun -keyalg RSA -keysize 2048 -validity 10000
```
چند سوال (رمز عبور، نام و...) از تو می‌پرسد؛ جواب بده. **فایل `camelrun-release.keystore` و رمزهایی که وارد کردی را در جای امن نگه‌دار** — اگر گم شود، دیگر هرگز نمی‌توانی همین برنامه را آپدیت کنی.

### مرحله ۲: ساخت AAB (فرمت پیشنهادی)
```
cordova build android --release -- --packageType=bundle
```
فایل خروجی:
```
platforms/android/app/build/outputs/bundle/release/app-release.aab
```

### مرحله ۳: امضای فایل
```
jarsigner -verbose -sigalg SHA256withRSA -digestalg SHA-256 -keystore camelrun-release.keystore platforms/android/app/build/outputs/bundle/release/app-release.aab camelrun
```

اگر ترجیح می‌دهی APK بسازی (به‌جای AAB)، به‌جای مرحله ۲:
```
cordova build android --release
```
و فایل خروجی در:
```
platforms/android/app/build/outputs/apk/release/app-release-unsigned.apk
```
که باید همان‌طور که در مرحله ۳ گفته شد امضا شود (مسیر فایل را به‌جای مسیر AAB بگذار)، و سپس با ابزار `zipalign` بهینه شود:
```
zipalign -v 4 app-release-unsigned.apk camelrun-final.apk
```

---

## بخش ۷: آماده‌سازی برای انتشار در کافه‌بازار

۱. در سایت **console.cafebazaar.ir** یک حساب توسعه‌دهنده بساز.
۲. یک «برنامه جدید» ایجاد کن.
۳. اطلاعات لازم را آماده کن:
   - نام و توضیح کوتاه/بلند فارسی برنامه.
   - حداقل ۲ اسکرین‌شات از بازی (با گرفتن عکس از صفحه گوشی هنگام اجرای بازی).
   - آیکون ۵۱۲×۵۱۲ برای صفحه فروشگاه.
   - دسته‌بندی: «بازی» → زیردسته مناسب (مثلاً Arcade/Casual).
   - رده سنی محتوا.
۴. فایل AAB یا APK امضا شده‌ای که در بخش ۶ ساختی را آپلود کن.
۵. حریم خصوصی: اگر بعداً تبلیغات یا خرید درون‌برنامه‌ای اضافه کردی، باید یک متن «سیاست حریم خصوصی» هم آپلود کنی (کافه‌بازار لینکش را می‌خواهد).
۶. برنامه را برای بررسی ارسال کن.

---

## بخش ۸: افزودن تبلیغات و خرید درون‌برنامه‌ای در آینده

کد بازی از قبل آماده‌ی این کار است. در فایل `www/index.html` این توابع را پیدا کن:

- `showBannerPlaceholder()` → محل نمایش تبلیغ بنری (پایین صفحه). عنصر `#bannerAdSlot` هم آماده است.
- `showInterstitialPlaceholder()` → به‌طور خودکار هر ۳ بار باخت یک‌بار صدا زده می‌شود؛ محل تبلیغ بین‌مرحله‌ای.
- `showRewardedPlaceholder(callback)` → برای تبلیغ جایزه‌ای (مثلاً «ادامه بازی» بعد از باخت).
- `localStorage` کلید `camelRun_removeAds` → وقتی مقدارش `'1'` باشد، هیچ تبلیغی نشان داده نمی‌شود (برای خرید درون‌برنامه‌ای «حذف تبلیغات»).

برای اضافه کردن تبلیغات واقعی کافه‌بازار، از سرویس **باما (Bama Ads)** یا از AdMob استفاده کن؛ پلاگین Cordova مربوطه را نصب کن (مثلاً `cordova-plugin-admob-plus`) و کد نمایش تبلیغ SDK را داخل همان توابع بالا جایگزین کن.

برای خرید درون‌برنامه‌ای، از **پلاگین پرداخت کافه‌بازار (IAB)** استفاده کن؛ مستندات آن در سایت توسعه‌دهندگان کافه‌بازار موجود است.

---

## بخش ۹: تست نهایی قبل از انتشار (چک‌لیست)

- [ ] فایل APK/AAB نهایی را روی حداقل ۲ گوشی متفاوت (یکی ضعیف، یکی معمولی) نصب و تست کن.
- [ ] گوشی را در حالت «هواپیما» بگذار و مطمئن شو بازی کاملاً بدون اینترنت اجرا می‌شود.
- [ ] دکمه‌ی صدا (🔊/🔇) را تست کن و مطمئن شو حالتش بعد از بستن و باز کردن برنامه حفظ می‌شود.
- [ ] چند بار ببازی و مطمئن شو رکورد (بهترین امتیاز) درست ذخیره و نمایش داده می‌شود.
- [ ] دکمه «دوباره بازی» و «شروع بازی» را چک کن.
- [ ] بازی را در حالت افقی نگه دار — چون برنامه روی «عمودی» قفل شده، نباید بچرخد.
- [ ] چند دقیقه پشت سر هم بازی کن تا مطمئن شوی سرعت و سختی به‌درستی افزایش پیدا می‌کند و بازی کند/هنگ نمی‌شود.

موفق باشی! 🐫
